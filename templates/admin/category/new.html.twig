{% extends 'admin/layout.html.twig' %}

{% block title %}Nuova categoria — Blog CMS{% endblock %}

{% block body %}
    <div class="mb-6">
        <a href="{{ path('admin_category_index') }}" class="text-sm text-gray-500 hover:text-indigo-600 transition">← Torna alla lista</a>
    </div>
    <h1 class="text-2xl font-bold text-gray-900 mb-6">Nuova categoria</h1>
    {{ include('admin/category/_form.html.twig') }}
{% endblock %}
